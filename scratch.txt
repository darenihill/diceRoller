Crow, Basket, Red, Yellow, Green, Blue

.icon,
.menu-icon {
        font-family: "Font Awesome 6 Free";
        font-size: calc(var(--primary-button-size) * 0.65);
        /* Updated from 0.65 to 0.5 */
        color: var(--btn-color);
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100%;
        width: 100%;
}

.menu-icon {
        font-size: calc(var(--primary-button-size) * 0.4);
}